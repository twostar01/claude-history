---
phase: 01-scaffolding-schema-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/claude_history/schema_discovery.py
  - .planning/SCHEMA.md
autonomous: true
requirements:
  - INGEST-01

must_haves:
  truths:
    - "uv run schema-discovery <zip> prints top-level keys, conversation fields, message fields, and timestamp formats without error"
    - ".planning/SCHEMA.md is written and contains the project-association gap warning"
    - "schema_discovery.py makes no database writes — it is read-only"
    - "schema_discovery.py can run independently without server.py or config.py"
  artifacts:
    - path: "src/claude_history/schema_discovery.py"
      provides: "ZIP inspection script that produces SCHEMA.md"
      exports: ["main"]
    - path: ".planning/SCHEMA.md"
      provides: "Field reference for Phase 2 ingest.py authoring"
      contains: "project association"
  key_links:
    - from: "schema_discovery.py"
      to: ".planning/SCHEMA.md"
      via: "schema_path.write_text()"
      pattern: "write_text"
    - from: "schema_discovery.py"
      to: "data-030cd706-*.zip"
      via: "zipfile.ZipFile(zip_path)"
      pattern: "zipfile\\.ZipFile"
---

<objective>
Write schema_discovery.py that reads the real Claude.ai export ZIP in-memory and produces a Markdown field reference at .planning/SCHEMA.md — the document that unblocks Phase 2 ingest planning.

Purpose: Phase 2 cannot be planned or implemented without knowing the exact field names, types, and structural quirks (especially the project-association gap) of the real export format. schema_discovery.py materializes this knowledge from the actual ZIP.
Output: schema_discovery.py (runnable via `uv run schema-discovery`) and .planning/SCHEMA.md written from the real export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffolding-schema-discovery/01-CONTEXT.md
@.planning/phases/01-scaffolding-schema-discovery/01-RESEARCH.md
@.planning/phases/01-scaffolding-schema-discovery/01-01-SUMMARY.md

<interfaces>
<!-- Export ZIP schema pre-verified in RESEARCH.md — executor has this as ground truth. -->
<!-- Do NOT re-inspect the ZIP to derive these; write schema_discovery.py to PRODUCE them. -->

The real export ZIP (data-030cd706-...zip) contains these members (verified 2026-05-04):
  conversations.json          — array of 106 conversation objects
  projects/UUID.json          — 6 files, one per project (single dict per file)
  design_chats/UUID.json      — 2 files, one per design chat
  users.json                  — array with 1 user object

conversations.json conversation object fields:
  uuid (str), name (str), summary (str), created_at (str ISO-Z),
  updated_at (str ISO-Z), account (object), chat_messages (array)

chat_messages[n] fields:
  uuid (str), text (str), content (array), sender (str "human"|"assistant"),
  created_at (str ISO-Z), updated_at (str ISO-Z), attachments (array),
  files (array), parent_message_uuid (str)

projects/UUID.json fields:
  uuid (str), name (str), description (str), is_private (bool),
  is_starter_project (bool), prompt_template (str),
  created_at (str ISO +00:00), updated_at (str ISO +00:00),
  creator (object), docs (array)

design_chats/UUID.json fields:
  uuid (str), title (str), project (object {uuid, name}),
  created_at (str ISO +00:00), updated_at (str ISO +00:00),
  messages (array of complex message objects)

CRITICAL GAP: conversations.json entries have NO project field.
Project association is NOT available from conversations.json alone.
design_chats DO have a project field. Phase 2 must decide how to handle this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write schema_discovery.py</name>
  <files>src/claude_history/schema_discovery.py</files>

  <read_first>
    - .planning/phases/01-scaffolding-schema-discovery/01-RESEARCH.md (Pattern 4: Schema Discovery with zipfile, Discovered Export Schema section — all subsections, D-03 and D-04 from CONTEXT.md, Open Question 2 and 3)
    - .planning/phases/01-scaffolding-schema-discovery/01-CONTEXT.md (D-03: print + write SCHEMA.md, D-04: top-level keys + field names/types + one example + timestamp format, D-05: Markdown only)
  </read_first>

  <action>
Create `src/claude_history/schema_discovery.py` implementing these behaviors:

**CLI interface:**
```
Usage: uv run schema-discovery <path-to-export.zip>
```
- Accepts one positional argument: path to ZIP file
- Prints usage to stderr and exits 1 if arg is missing or file does not exist
- All diagnostic/progress output goes to stderr (standard print() is fine here since this is a CLI tool, NOT the MCP server)

**What to inspect and document (per D-04):**

For each JSON member in the ZIP:
1. Print the member filename
2. Load the JSON and determine if it is an array or object
3. For each field: name, inferred type (str/int/bool/object/array/null), one truncated example value

**SCHEMA.md structure (per D-05, D-04):**

```markdown
# Claude.ai Export Schema

Discovered from: <zip filename>
Discovered at: <ISO timestamp>
Export contains: <N> files

## ZIP File Structure

<table: member name, type (array/object), count if array>

## conversations.json — Conversation Object

<table: field, type, example>

## conversations.json — Message Object (chat_messages[n])

<table: field, type, example>

## projects/UUID.json — Project Object

<table: field, type, example>
Note: Timestamp format uses +00:00 offset (not Z suffix)

## design_chats/UUID.json — Design Chat Object

<table: field, type, example>

## users.json — User Object

<table: field, type, example>

## Timestamp Formats

- conversations.json: ISO 8601 with Z suffix (e.g., "2026-03-28T05:52:32.764454Z")
- projects/UUID.json: ISO 8601 with +00:00 offset (e.g., "2026-03-05T06:20:46.561314+00:00")
- Python 3.11+ datetime.fromisoformat() handles both formats

## Project Association Gap

**WARNING for Phase 2:** `conversations.json` entries do NOT contain a `project` field.
There is no direct way to associate a conversation with a project from conversations.json alone.

- `projects/UUID.json` contains project metadata but no list of conversation UUIDs
- `design_chats/UUID.json` DOES contain a `project` field (object with uuid and name)
- Regular conversations in conversations.json are NOT linked to projects in this export format

**Phase 2 decision required:** Either (a) skip project association for conversations and store NULL,
or (b) attempt to infer project association from design_chats. Document the chosen approach in Phase 2 planning.
```

**Implementation notes:**

- Use `zipfile.ZipFile(zip_path)` (stdlib) to open the ZIP without extracting
- Use `json.load(zf.open(member_name))` to parse JSON members in-memory
- The schema path computation: `Path(__file__).parent.parent.parent.parent / ".planning" / "SCHEMA.md"`
  (schema_discovery.py is at src/claude_history/ = 2 levels from src/, 3 from project root, plus .planning/ subdirectory)
  Actually: `Path(__file__)` = `src/claude_history/schema_discovery.py`
  `.parent` = `src/claude_history/`
  `.parent.parent` = `src/`
  `.parent.parent.parent` = project root
  So: `Path(__file__).parent.parent.parent / ".planning" / "SCHEMA.md"`

- Example value truncation: truncate strings to 60 chars with `...` suffix; show raw value for non-strings

- For design_chats: inspect the first design chat file's fields (don't inspect all — one is enough for schema). Do capture that `project` field IS present.

- The script writes SCHEMA.md and also prints the full content to stdout (CLI tool, not the MCP server — print() is correct here per D-03)

- Wrap the entire main() body in a try/except that prints errors to stderr

**Structural outline for main():**

```python
def main() -> None:
    if len(sys.argv) < 2:
        print("Usage: uv run schema-discovery <path-to-export.zip>", file=sys.stderr)
        sys.exit(1)

    zip_path = Path(sys.argv[1])
    if not zip_path.exists():
        print(f"Error: file not found: {zip_path}", file=sys.stderr)
        sys.exit(1)

    with zipfile.ZipFile(zip_path) as zf:
        # 1. List all members
        # 2. Load conversations.json — inspect first conversation + first message
        # 3. Load each projects/*.json — inspect first project
        # 4. Load each design_chats/*.json — inspect first design chat + messages key
        # 5. Load users.json — inspect first user
        # 6. Build schema_content (Markdown string)

    schema_path = Path(__file__).parent.parent.parent / ".planning" / "SCHEMA.md"
    schema_path.write_text(schema_content, encoding="utf-8")

    print(schema_content)  # D-03: also print to console
    print(f"\nSchema written to: {schema_path}", file=sys.stderr)
```

Add `if __name__ == "__main__": main()` guard at the bottom.
  </action>

  <verify>
    <automated>uv run python -c "import py_compile; py_compile.compile('src/claude_history/schema_discovery.py', doraise=True); print('syntax ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `src/claude_history/schema_discovery.py` exists
    - File contains the string `zipfile.ZipFile`
    - File contains the string `def main`
    - File contains the string `SCHEMA.md`
    - File contains the string `write_text`
    - File contains the string `__name__ == "__main__"`
    - `uv run python -c "import py_compile; py_compile.compile('src/claude_history/schema_discovery.py', doraise=True)"` exits 0 (no syntax errors)
    - File does NOT import from `claude_history.config` or `claude_history.server` (standalone script)
  </acceptance_criteria>

  <done>schema_discovery.py is syntactically valid, reads the ZIP via stdlib zipfile, builds a Markdown schema document, writes it to .planning/SCHEMA.md, and prints it to stdout.</done>
</task>

<task type="auto">
  <name>Task 2: Run schema_discovery.py against the real export ZIP</name>
  <files>.planning/SCHEMA.md</files>

  <read_first>
    - src/claude_history/schema_discovery.py (just written — verify it exists before running)
    - .planning/phases/01-scaffolding-schema-discovery/01-RESEARCH.md (ZIP file structure section — use this to verify SCHEMA.md output matches known structure)
  </read_first>

  <action>
Step 1 — Run schema discovery against the real export ZIP at the project root:

```powershell
uv run schema-discovery "data-030cd706-d473-447c-a7fe-b6d98e4f1277-1777822473-5f91f1fb-batch-0000.zip"
```

Step 2 — Verify the script printed output to console and created `.planning/SCHEMA.md`.

Step 3 — Read the generated `.planning/SCHEMA.md` and confirm it contains:
- A "ZIP File Structure" section listing conversations.json, projects/, design_chats/, users.json
- A "conversations.json — Conversation Object" section with uuid, name, chat_messages fields
- A "conversations.json — Message Object" section with uuid, text, sender, created_at fields
- A "Timestamp Formats" section documenting both `Z` and `+00:00` variants
- A "Project Association Gap" section explicitly flagging the missing project field on conversations

Step 4 — If any of the required sections are missing, edit schema_discovery.py to add them, then re-run. The goal is a SCHEMA.md that a future Phase 2 executor can use to write ingest.py field extraction without re-opening the ZIP.

Step 5 — Verify the export ZIP was not modified by the script:

```powershell
# Confirm ZIP still exists and schema_discovery.py did not write to it
ls "data-030cd706-d473-447c-a7fe-b6d98e4f1277-1777822473-5f91f1fb-batch-0000.zip"
```

Step 6 — Verify no database file was created (schema_discovery.py must be read-only):

```powershell
ls *.db 2>$null
# Should output nothing — history.db must NOT exist after schema discovery
```
  </action>

  <verify>
    <automated>python -c "from pathlib import Path; content = Path('.planning/SCHEMA.md').read_text(); required = ['conversations.json', 'chat_messages', 'sender', 'Timestamp', 'Project Association Gap', 'project']; missing = [r for r in required if r not in content]; print('MISSING:', missing) if missing else print('SCHEMA.md ok - all required sections present')"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/SCHEMA.md` exists and is non-empty (file size > 500 bytes)
    - `.planning/SCHEMA.md` contains the string `conversations.json`
    - `.planning/SCHEMA.md` contains the string `chat_messages`
    - `.planning/SCHEMA.md` contains the string `sender`
    - `.planning/SCHEMA.md` contains the string `Timestamp`
    - `.planning/SCHEMA.md` contains the string `Project Association Gap` (or equivalent warning about missing project field)
    - `.planning/SCHEMA.md` contains the string `project` (in the context of the gap warning)
    - `.planning/SCHEMA.md` contains the string `design_chats`
    - No `history.db` file exists at the project root (script made no DB writes)
    - The export ZIP still exists and is unmodified
  </acceptance_criteria>

  <done>.planning/SCHEMA.md is written from the real export ZIP, contains all required sections including the project-association gap warning, and no database was created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ZIP file → Python process | Untrusted ZIP content parsed by stdlib zipfile (no arbitrary code execution risk) |
| schema_discovery.py → filesystem | Writes only to .planning/SCHEMA.md; must NOT write to any .db file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | .planning/SCHEMA.md | accept | SCHEMA.md contains only field names and truncated example values, not full conversation content; low sensitivity |
| T-01-03-02 | Tampering | schema_discovery.py filesystem writes | mitigate | Script only calls write_text() on the single .planning/SCHEMA.md path; no DB writes; no ZIP modification; verified by checking history.db does not exist after run |
| T-01-03-03 | Denial of Service | malformed ZIP | accept | stdlib zipfile raises BadZipFile on malformed input; script wraps in try/except; personal tool, not a public endpoint |
</threat_model>

<verification>
After both tasks complete:

1. `uv run schema-discovery "data-030cd706-...zip"` exits 0 and prints schema to console
2. `.planning/SCHEMA.md` exists with content > 500 bytes
3. SCHEMA.md contains: `conversations.json`, `chat_messages`, `sender`, `Timestamp`, `Project Association Gap`, `design_chats`
4. No `history.db` file at project root (schema_discovery.py is read-only)
5. Export ZIP is unmodified
6. `uv run python -c "import py_compile; py_compile.compile('src/claude_history/schema_discovery.py', doraise=True)"` exits 0
</verification>

<success_criteria>
- schema_discovery.py runs without error against the real export ZIP
- .planning/SCHEMA.md is written and contains: conversation fields, message fields, timestamp format documentation, AND the project-association gap warning
- Zero database writes — no history.db created
- Phase 2 executor can read SCHEMA.md alone and write ingest.py without re-opening the ZIP
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-schema-discovery/01-03-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.

Key items to record in the summary:
- The exact ZIP filename used and its member count
- Any fields discovered that were NOT in RESEARCH.md (unexpected schema additions)
- The final .planning/SCHEMA.md path as an absolute path
- Whether design_chats were fully documented (required for Phase 2 decision)
- The project-association gap section text (verbatim) as it appears in SCHEMA.md
</output>
