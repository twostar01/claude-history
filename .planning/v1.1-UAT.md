---
status: complete
phase: v1.1-milestone
source: [05-01-SUMMARY.md, 06-01-SUMMARY.md]
started: 2026-05-18T00:00:00Z
updated: 2026-05-18T00:00:00Z
---

## Current Test

[testing complete — 10/10 passed]

## Tests

### 1. Cold Start — MCP server responds
expected: Restart or let Claude Code re-spawn the MCP server and make any tool call (e.g. get_status). Server starts without errors and returns a valid response.
result: pass

### 2. search_conversations — date_from filter
expected: Call search_conversations with a query you know exists AND a date_from set to yesterday's date (2026-05-17). You should get results from today/yesterday. Then call with date_from="2099-01-01" — you should get 0 results.
result: pass

### 3. search_conversations — date_to filter
expected: Call search_conversations with a query and date_to="2020-01-01". You should get 0 results (all conversations are newer than 2020).
result: pass

### 4. search_conversations — role_filter human
expected: Call search_conversations with role_filter="human" and any common word. Every result snippet should come from a human message (your messages, not Claude's replies).
result: pass
note: Output shows conversation-level results not message snippets, but filter verified by comparing with assistant filter — different conversations and significantly different match counts (24 human vs 59 assistant for "Open source animal detection"), confirming filter is applied correctly.

### 5. search_conversations — role_filter assistant
expected: Call search_conversations with role_filter="assistant" and any common word. Every result snippet should come from an assistant message.
result: pass
note: Verified alongside Test 4. Assistant matches consistently higher counts (more Python in Claude responses than user messages), and different conversation set returned.

### 6. export_conversation — write to file
expected: Call export_conversation with a valid conversation id AND file_path="C:/Users/nclem/Desktop/test-export.md". The tool should return "Written to: C:\Users\nclem\Desktop\test-export.md" and the file should exist on disk with markdown content.
result: pass

### 7. export_conversation — backward compat (no file_path)
expected: Call export_conversation with just an id and no file_path. Should return markdown content as a string, same as before v1.1.
result: pass
note: Conversation too large to display inline but returned markdown (not "Written to:" path), confirming backward-compat path is correct.

### 8. Ingest re-run — same ZIP, no duplication
expected: Run `! uv run ingest <path-to-your-export.zip>` (same ZIP you've already ingested). Log should show "0 new conversations", "0 existing conversations updated (0 new messages)", and "N conversations unchanged". No errors.
result: pass
reported: "0 new conversations / 0 existing conversations updated (0 new messages) / 106 conversations unchanged"

### 9. search_conversations — still works after ingest changes
expected: After the re-run ingest, call search_conversations with any query. Should return results normally — ingest changes haven't broken search.
result: pass

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

### 10. Incremental ingest — new messages actually appended
expected: Export fresh history from claude.ai (you've had new conversations since May 16). Run `uv run ingest <new-export.zip>`. Log should show at least some "existing conversations updated (N new messages)" — not all zeros. Then `get-status` should show a higher message count than 114.
result: pass
reported: "115 conversations found / 1 new conversations / 1 existing conversations updated (2 new messages) / 113 conversations unchanged"

## Gaps

